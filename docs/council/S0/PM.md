# PM 재산정안: 신규 REQ 반영 S0~S6 일정 (2026-09-19, 협의체 1차)

> 역할: PM. 입력: BRIEF, `docs/REQUIREMENTS.md`, `docs/16_실행계획.md`, 12번·14번·15번 설계, `apps/ios-macos/Project.swift`, `gh issue list`(#1~#30), `gh api milestones`.
> 원칙: 계약 v1.0 불변, 무료 티어만(REQ-11), 1인, v1.0 = 2026-11-06. 모르는 것은 "확인 필요".

---

## 0. 현황 사실 (근거)

| 항목 | 사실 | 출처 |
|---|---|---|
| 이슈 | #1 CLOSED(S0), #2~#30 OPEN. 마일스톤 S0(9/26)~S6(11/6) 7개, S6는 이슈 0개 | `gh issue list`, `gh api milestones` |
| Day 1 | PR #31(골격·ADR 0001~0004), PR #32(REQ 대장) 이미 머지 → Day 1은 9/19에 선완료 | `gh pr list` |
| 앱 타깃 | 모든 모듈·앱 `destinations: [.iPhone, .iPad, .mac]`, `deploymentTargets iOS 26 / macOS 26` | `Project.swift` |
| 코드 상태 | 모듈 8개는 플레이스홀더 1파일씩(`Domain.swift` 등), `eval/`는 README만, `contracts/push-payload.json` 없음(14번 §16에는 있음) | 파일 목록 |
| 달력 | 9/22는 **화요일**, 9/26은 **토요일**(16번은 "9/22(월)"로 표기 — 오기). S0~S5 평일 = 4+5+5+5+5+5 = **29일**, 계획은 Day 1~30 = 30일 → Day 5가 토요일에 걸림. S6 평일 5 | `cal 9 2026` |
| 공휴일 | 추석 연휴(9/24~26 추정)·대체공휴일(9/28 추정)·개천절 대체(10/5 추정)·한글날(10/9)이 S0~S2에 겹침 — **확인 필요**(사용자가 근무일로 쓸지 결정) | 확인 필요 |
| Apple API | `sidebarAdaptable` **iOS 18/macOS 15+**; `SpeechAnalyzer` **iOS 26+**; `SystemLanguageModel`(Foundation Models) **iOS 26+ + Apple Intelligence 기기**; `@Entry` **iOS 13+**(폴백 불필요); `SFSpeechRecognizer` iOS 10+(1분 제한, 네트워크 의존 가능) | sosumi 2026-09-19 |

**시사점**: 신규 REQ 이전에도 달력상 1일 부족(+공휴일 최대 5일). 신규 REQ 순증은 아래 §1 합계 **+10.5일**. 기한(11/6)과 인원(1)이 고정이므로 답은 **범위 감축**뿐이다(§3).

---

## 1. 스프린트별 추가/변경 작업 (0.5일 = 3h 단위)

하루 6h 기준(16번 §4). 견적 근거는 "무엇을 새로 만드나 / 이미 있는 것에 얹나"로 적었다.

### S0 뼈대 (9/22~9/26) — 순증 +1.0일
| ID | 작업 | 견적 | 왜 이 견적인가 | REQ |
|---|---|---|---|---|
| S0-A | `Project.swift` deploymentTargets **iOS 17 / macOS 14**(테스트·Catalog 포함 19개 타깃), `SWIFT_STRICT_CONCURRENCY` 유지, 컴파일 확인. ADR-0005 "T-C2 재결정(26 only → 17+)" | 0.5 | 값 치환은 10분이지만, 플레이스홀더라도 `@available` 없이 26 API를 쓴 코드가 없는지 빌드 2회(iOS·Mac) + ADR 한 장 | REQ-02 |
| S0-B | `Domain` 타깃 destinations에 `.appleWatch` 추가 시도(Foundation만 의존하므로 통과해야 함). 실패 시 이슈로만 남기고 제거 | 0(Day 2에 흡수) | 한 줄. 통과 여부가 "워치 공유 가능" 증거가 된다 | REQ-04 |
| S0-C | archify 초기 3장: 모듈 그래프(12번 §0), 해석 요청 흐름(14번 §5-1), 녹음 상태 기계(12번 §7) → `docs/diagrams/` | 0.5 | 그림당 ~1h(요구사항 → SVG → 검토). 이후 스프린트 그림은 이 3장을 갱신하는 방식이라 초기 투자 | REQ-24 |
| S0-D | Day 5의 "블루그린 스크립트 검증"을 Day 30으로 이동 | −0.25 | Day 5가 이미 4항목(Domain·UI 토큰·TabCoordinator·블루그린)으로 과적재. 스크립트는 ADR-0003으로 설계 끝, 실서버(S5) 전에는 검증 의미가 약함 | — |

### S1 시작 (9/28~10/2) — 순증 +1.0일
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S1-A | `AdaptiveRoot` 이중 구현: `#available(iOS 18, macOS 15)`면 `TabView + .sidebarAdaptable`, 아니면 iPhone `TabView(탭바)` / iPad·Mac `NavigationSplitView`. 둘 다 `TabCoordinator.paths`·`selectedTab`을 같은 바인딩으로 사용 | 1.0 | 루트 뷰 2벌 + 선택 상태 공유 + 두 OS 시뮬에서 확인. 12번 §8 "루트 뷰는 하나" 결정이 "루트 래퍼는 하나, 구현은 둘"로 바뀌는 지점이라 D-17 테스트도 손봐야 함 | REQ-01·02 |
| S1-B | "넓은 화면" 규칙 확정: `isWide = horizontalSizeClass == .regular || os(macOS)`를 Environment(`@Entry var mwIsWide`)로 내려 D-2 분기(A-06 접기, W-01 시트/화면)가 플랫폼이 아니라 이 값을 보게 | 0(S1-A에 포함) | `@Entry`는 iOS 13+ 이므로 폴백 없음. 처음부터 이 값을 쓰면 화면별 추가 비용 0 | REQ-01·03 |
| S1-C | 협의체 S1 킥오프(§5 규칙) | 0.25 | 사람 시간 1.5h(에이전트 산출물 읽고 결정) | REQ-25 |

### S2 해석 (10/5~10/9) — 순증 +3.5일 (가장 큰 순증, §3에서 1.0 감축)
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S2-A | `MwonmalUI/Motion`: `MwMotion` 토큰(duration 3단·spring response/damping·easing), `MwTransition` 프리셋, `@Entry var mwMotion`로 컨테이너 단위 튜닝, `accessibilityReduceMotion`이면 전부 `.none`. 단위 테스트(토큰 값·reduceMotion 스위치) | 0.5 | 코어는 값 타입 + 환경 키. "코어(곡선·물리·지속시간)까지 손댈 수 있게"는 spring 파라미터 노출로 충족 | REQ-22 |
| S2-B | Catalog "Motion 튜닝" 페이지(슬라이더로 실시간 값 변경) | 0.5 → **v1.1** | 있으면 좋지만 REQ-22의 "튜닝 가능"은 코드 파라미터로 이미 충족 | REQ-22 |
| S2-C | `eval/run.py` 평가 하네스: 어댑터 인터페이스 1개 + ①API(Gemini Flash 무료, Groq 무료; OpenAI·Claude는 사용자 키 있을 때만) | 0.5 | Day 11의 라우터 코드와 프롬프트를 재사용. 점수 함수(정확 일치·부분·JSON 유효율·지연·비용)는 14번 §15에 정의됨 | REQ-10·11 |
| S2-D | ③ Apple MLX 어댑터(`mlx-lm`, Qwen3 4B 4bit, 내 Mac) 50문장 실측 | 0.5 | 모델 다운로드 수 GB + 프롬프트 포맷 맞추기 + 실행. 시간 초과 시 "미측정"으로 기록하고 종료(타임박스) | REQ-10 |
| S2-E | ② Foundation Models 어댑터(macOS 26 CLI, guided generation) — **개발 Mac이 macOS 26 + Apple Intelligence 활성일 때만**(확인 필요) | 0.5 → **조건부/v1.1** | 기기 조건 미충족이면 실측 불가. 14번 §15도 온디바이스는 v1.1 후보 | REQ-10 |
| S2-F | ④ 자체 sLLM(nanoGPT/LoRA): 데이터 2~3천 개가 필요(14번 §15 파인튜닝 트랙 1)해서 v1.0 안에 불가. v1.0은 "베이스 Qwen3 4B(③)"를 ④의 하한선으로 기록 | 0 → **v1.1** | 비교 기준(평가셋 200)이 먼저라는 14번 §15 순서 그대로 | REQ-10 |
| S2-G | `eval/cost_model.py`: 입력(MAU, 1인당 해석·녹음·복습/월, 트랙별 단가·무료 한도) → 출력(월 원가, 트랙별 분해, 손익분기 가격). 단가·한도는 `eval/prices.yml`(날짜 표기), pytest 3케이스 | 0.5 | 산식은 곱셈·합. 어려운 건 단가 수집 — Gemini 무료 티어 RPM/RPD, STT 단가는 **확인 필요**로 두고 자리표시 | REQ-13 |
| S2-H | ADR-0007 "AI 라우터 v1.0 = API 무료 티어, 온디바이스·MLX·sLLM 결과와 비용 모델 첨부" + archify 1장(4트랙 비교 흐름) | 0.5 | 점수표·비용표를 REQ-20 형식으로 옮기는 문서 작업 | REQ-12·20·24 |
| S2-I | 협의체 S2 킥오프 | 0.25 | | REQ-25 |

### S3 회의 (10/12~10/16) — 순증 +0.75일
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S3-A | `contracts/push-payload.json` 스키마(`type` 4종, `translationId/meetingId/projectId/quizId`, `deepLink` 문자열, **텍스트 필드 없음**) + 서버 `notify`가 이 스키마로 직렬화 + 앱 `DeepLink.parse` 입력을 이 스키마로 고정. ADR-0006 "알림 payload는 워치가 그대로 소비 가능" | 0.5 | 12번 §13 백로그의 "알림 payload 스키마"를 계약 밖 파일로 앞당기는 것. 서버 Day 18이 어차피 payload를 만들므로 스키마 문서화+검증 테스트만 순증 | REQ-04 |
| S3-B | Day 19 `SpeechAnalyzer` 미리보기 → `@available(iOS 26, macOS 26)` 스텁만 남기고 구현 **v1.1**. 17~25는 파형+"받아쓰기 준비 중"(D-7이 이미 정의한 상태) | −1.0 | §3 감축. SFSpeechRecognizer 폴백(1분 제한 → 60초 청크마다 태스크 재시작)은 +1.0이 더 드는데 D-7상 "진실은 서버 transcript"라 제품 가치가 미리보기 UX뿐 | REQ-02 |
| S3-C | 협의체 S3 킥오프 | 0.25 | | REQ-25 |

### S4 익히기 (10/19~10/23) — 순증 +0.25일
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S4-A | Motion 토큰을 ReviewCard 뒤집기·LevelStairs·StreakBadge에 적용(하드코딩 0) | 0(Day 22·23에 흡수) | S2-A가 있으면 값만 참조 | REQ-22 |
| S4-B | 협의체 S4 킥오프 | 0.25 | | REQ-25 |

### S5 live (10/26~10/30) — 순증 +0.75일
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S5-A | `cost_model` 입력을 실측값(Day 29 Amplitude 이벤트·서버 라우터 지표)으로 갱신, `make cost MAU=1000` 출력 표를 README에 | 0.5 | 코드는 S2-G. 측정값 수집 + 표 갱신 | REQ-13 |
| S5-B | 협의체 S5 킥오프 | 0.25 | | REQ-25 |

### S6 마감 (11/2~11/6) — 순증 +1.25일 (§3 감축으로 상쇄)
| ID | 작업 | 견적 | 왜 | REQ |
|---|---|---|---|---|
| S6-A | 스냅샷 매트릭스 확장: iPhone SE(4.7")·iPad 13"(regular)·iPad Split View(compact) + iOS 17 시뮬레이터 런타임으로 흐름 4개 서브셋 | 1.0 | 기기 3종 × 흐름 4 = 12장 갱신 + iOS 17 런타임 설치·CI job 1개. **Xcode 26에서 iOS 17 시뮬 런타임 설치 가능 여부 확인 필요** | REQ-01·02·03 |
| S6-B | 협의체 S6 킥오프 + 릴리스 레드팀 | 0.25 | | REQ-25 |

**순증 합계(감축 전)**: S0 1.0 + S1 1.25 + S2 3.75 + S3 0.75 + S4 0.25 + S5 0.75 + S6 1.25 = **+9.0일**, 여기에 S2-B·S2-E를 v1.1로 빼기 전 값(+1.0)을 더하면 **+10.5일**. 달력 결손(§0) −1일. → 감축 목표 **≥ 10일**(§3).

---

## 2. Day 2~30 변경표 (이슈 #2~#30 기준)

범례: **유지** / **수정**(제목·범위 변경) / **추가**(새 이슈) / **삭제**(v1.1 이관; 이슈는 닫지 말고 `sprint:v1.1` 라벨 + 마일스톤 해제 권고).

| Day(날짜) | 이슈 | 현재 제목(요약) | 조치 | 변경 내용·근거 |
|---|---|---|---|---|
| 2 (9/22 화) | #2 | Tuist 그래프 8+2+Catalog+테스트 | **수정** | + deploymentTargets **iOS 17/macOS 14** 전 타깃, `Domain`에 `.appleWatch` 시도(S0-B), ADR-0005. 견적 M→L |
| 3 (9/23 수) | #3 | MwonmalAPI 생성·43 op·KST·미들웨어 | 유지 | 생성 코드는 OS 무관 |
| 4 (9/24 목) | #4 | FastAPI 뼈대·17테이블·/levels·Compose | 유지 | 공휴일 여부 확인 필요 |
| 5 (9/25 금) | #5 | Domain·UI 토큰·TabCoordinator·블루그린 | **수정** | − 블루그린 검증(→ #30). + archify 초기 3장(S0-C). 공휴일 여부 확인 필요 |
| (9/26 토) | — | — | **추가** | `[S0][docs] 협의체 S1 킥오프 + S0 데모 녹화` (0.5). 토요일이므로 저강도 문서 작업만 배치 |
| 6 (9/28 월) | #6 | OAuth 3종·JWT·/users/me | 유지 | 대체공휴일 여부 확인 필요 |
| 7 (9/29 화) | #7 | Onboarding·Stats·Levels·updateMe | 유지 | |
| 8 (9/30 수) | #8 | Platform OAuth·TokenStore·UseCase·Fake | 유지 | `ASWebAuthenticationSession`·Keychain은 17/14에서 동일 |
| 9 (10/1 목) | #9 | S-01 로그인·인트로·온보딩·딥링크 큐 | 유지 | |
| 10 (10/2 금) | #10 | A-01 홈·A-13/W-08·A-10·sidebarAdaptable 루트 | **수정** | "sidebarAdaptable 루트" → "**AdaptiveRoot 이중 구현(18/15+ sidebarAdaptable, 17/14 SplitView+TabView) + `mwIsWide` 규칙**". 견적 L → 1.5일 → **A-13/W-08(최근 회의)을 Day 20(#20)으로 이동**해 하루 유지 |
| 11 (10/5 월) | #11 | AI 라우터·사전 우선·POST /translations·10분 재사용 | **수정** | 라우터는 **Gemini Flash 무료 + Groq 폴백만**(Claude는 사용자 키 옵션, 기본 비활성 — REQ-11). "평가셋 v0 50문장"은 유지. 대체공휴일 여부 확인 필요 |
| 12 (10/6 화) | #12 | Term·Card(bulk)·GET translations·PATCH actions | 유지 | |
| 13 (10/7 수) | #13 | JargonText·ResultCard.Model·HighlightRange | **수정** | + `MwonmalUI/Motion` 코어(S2-A, 0.5). JargonText는 AttributedString 링크(iOS 15+) 그대로 |
| 14 (10/8 목) | #14 | W-01 Compose·Vision OCR·클립보드·TranslateUseCase | **수정** | − **Vision OCR → v1.1**(D-6은 유지, 구현만 이관). 절약 0.5 → 그 자리에 `eval/run.py` API 어댑터(S2-C) |
| 15 (10/9 금) | #15 | A-06/W-03 결과·카드 담기·용어 시트 | 유지 | 접기 분기는 `mwIsWide`. 한글날 여부 확인 필요 |
| (10/10 토) | — | — | **추가** | `[S2][docs] AI 4트랙 평가·비용 모델·ADR-0007` = S2-D(MLX 0.5) + S2-G(cost_model 0.5) + S2-H(ADR·그림 0.5) → **1.5일**. 평일에 자리가 없어 S2 마지막 슬롯으로 두되, 실제로는 **Day 16~18 서버 작업일의 앞뒤 1.5h씩 쪼개 넣는다**(서버 컨텍스트라 전환 비용 낮음) |
| 16 (10/12 월) | #16 | Project/Meeting CRUD·소유권·페이지네이션 | 유지 | + 협의체 S3 킥오프 1.5h |
| 17 (10/13 화) | #17 | Recording 파이프라인·S3·ARQ·상태 기계·ETag | 유지 | |
| 18 (10/14 수) | #18 | decode_meeting·DECODE_DONE·APNs·스케줄러 4·30일 삭제 | **수정** | + `contracts/push-payload.json` 스키마·검증 테스트·ADR-0006(S3-A, 0.5). 스케줄러는 4개 유지 |
| 19 (10/15 목) | #19 | AudioCaptureService·SpeechAnalyzer 미리보기·RecordingSession | **수정** | − **SpeechAnalyzer 미리보기 → v1.1**(`@available` 스텁 + "받아쓰기 준비 중" 상태만). 절약 1.0 → Day 10에서 넘어온 부담 흡수 + 버퍼 |
| 20 (10/16 금) | #20 | A-11/W-00·A-12/W-07·W-02a·W-02·라이브 액티비티·W-04·시트 체이닝 | **수정** | − **라이브 액티비티 → v1.1**(백그라운드 audio 모드 + 인앱 ● 표시로 대체, T-D1 되돌리기 조건과 동일). + A-13/W-08(Day 10에서 이동) |
| 21 (10/19 월) | #21 | Review(SM-2)·Quiz(오답 AI 캐시·등급표·409) | **수정** | − **오답 AI 생성 → 규칙(사전 다른 뜻 섞기)만**. 14번 §15 표의 폴백을 1순위로. 절약 0.5 → 협의체 S4 + 여유 |
| 22 (10/20 화) | #22 | ReviewCard SDK·A-03/04·AnswerCard·W-05 | **수정** | 애니메이션은 Motion 토큰만 사용(하드코딩 금지 체크리스트 항목 추가) |
| 23 (10/21 수) | #23 | A-07/08·LevelStairs·StreakBadge·quizResult | **수정** | 같음(Motion 토큰) |
| 24 (10/22 목) | #24 | 탭 전환·딥링크 4종·조상 복원·@SceneStorage | **수정** | − **@SceneStorage 복원 → v1.1**. 딥링크 파서 입력은 `push-payload.json` 스키마 |
| 25 (10/23 금) | #25 | A-09 설정·notification-settings·registerDevice | 유지 | + S4 데모 |
| 26 (10/26 월) | #26 | Terraform(OCI dev, AWS prod, R2)·cloud-init·sops | **수정** | − **AWS prod → v1.1**. v1.0 prod = OCI Always Free 두 번째 VM(또는 같은 VM의 별도 Compose 스택, 확인 필요). REQ-32 "무료 티어 우선" 충족, 비용 0 |
| 27 (10/27 화) | #27 | .live·401 dedupe·녹음 중 401 큐·ErrorPresenter | 유지 | + 협의체 S5 킥오프 1.5h |
| 28 (10/28 수) | #28 | iOS APNs 4종·액션 카테고리·macOS 로컬 알림·배지 | **수정** | − **macOS 로컬 알림(D-19) → v1.1**(계약 v1.1 `MACOS` 등록과 함께 한 번에). iPad는 iOS 빌드라 APNs 그대로 |
| 29 (10/29 목) | #29 | Sentry·Amplitude 15개·옵트아웃 | **수정** | Amplitude **15 → 6개**(온보딩 완료, 첫 해석, 카드 담기, 복습 완료, 시험 완료, 옵트아웃), Sentry 성능 트랜잭션 4 → 크래시+에러만. + cost_model 실측 갱신(S5-A, 0.5) |
| 30 (10/30 금) | #30 | prod 블루그린·백업·대시보드 4·계약 v1.1 초안 | **수정** | + 블루그린 스크립트 검증(Day 5에서). 대시보드 **4 → 2**(크래시율·p95). 계약 v1.1 초안에 `MACOS`, 알림 payload, refresh 토큰 포함 |
| S6 (11/2~11/6) | 없음 | S5 말에 세분화 | **추가** 5건 | ① `[S6][qa] 스냅샷 매트릭스(SE·iPad·Split·iOS17 시뮬)+Catalog 색맹 시뮬`(1.0) ② `[S6][qa] 접근성 감사 + Maestro 2흐름(로그인→해석, 녹음→결과)`(1.0; **4→2흐름, macOS XCUITest → v1.1**) ③ `[S6][server] k6 1시나리오(해석 30rps, AI는 스텁) + 복구 연습·런북`(1.0) ④ `[S6][app] iOS·iPad TestFlight + 심사 자료`(1.0) ⑤ `[S6][docs] v1.0 릴리스(app/v1.0.0, server/v1.0.0) + 협의체 릴리스 레드팀 + lessons`(1.0). **macOS TestFlight는 11/13(v1.0.1)로 분리**(§3 결정 PM-4) |

**수지 결산**: 순증 +10.5 / 감축 −(OCR 0.5 + STT 미리보기 1.0 + 라이브 액티비티 0.5 + 오답 AI 0.5 + SceneStorage 0.5 + AWS 0.5 + macOS 로컬 알림 0.5 + Amplitude·Sentry 0.75 + 대시보드 0.25 + Motion 카탈로그 0.5 + Foundation Models 어댑터 0.5 + Maestro 2흐름 0.5 + macOS XCUITest 0.5 + macOS TestFlight 이관 1.0) = **−8.5** / 달력 결손 −1 → **잔여 −3.0일**. 이것은 **토요일 2회(9/26, 10/10) 저강도 문서 작업 1.0 + Day 19 절약분 재배치 1.0 + S6 버퍼 없음 1.0**으로 메운다. 즉 **v1.0은 버퍼 0**이다. 공휴일을 쉬면 그만큼 §3 "다음 컷"이 자동 발동한다.

---

## 3. 크리티컬 패스와 잘라내는 것

### 3-1. 크리티컬 패스 (이 사슬이 밀리면 11/6이 밀린다)
```
앱:  #2(17/14 타깃) → #3(API 생성) → #8(인증) → #10(AdaptiveRoot 이중) → #14/#15(해석) → #19/#20(녹음) → #27(.live) → #28(APNs) → S6④ TestFlight
서버: #4(뼈대·마이그레이션) → #6(인증) → #11(라우터·평가셋) → #17/#18(녹음 파이프라인·알림) → #26(OCI prod) → #30(블루그린) → S6③
계약: push-payload.json(#18) → 딥링크 파서(#24) → APNs(#28)
```
- **가장 위험한 마디**: #10(루트 이중 구현). 여기서 하루 넘기면 S2 앱 3일(#13~#15)이 전부 밀린다. 그래서 A-13/W-08을 빼서 하루로 고정했다.
- **크리티컬 패스 밖**: AI 4트랙 평가(S2-C~H), 비용 모델, Motion 카탈로그, archify, 협의체. 이것들은 **밀려도 출시가 안 밀린다** → 그래서 토요일·서버 작업일 틈에 배치했고, 밀리면 S6 ⑤로 흘려보낸다(단 REQ-10·13은 "점수표·비용표 1장"이 v1.0 산출물이므로 완전 삭제는 불가).

### 3-2. v1.1 이관 후보 (이미 §2에 반영한 것 = 확정 제안, 나머지 = 다음 컷 순서)
| 순위 | 항목 | 절약 | 근거 | 상태 |
|---|---|---|---|---|
| 1 | SpeechAnalyzer 미리보기(D-7) | 1.0 | REQ-02로 17~25 사용자는 어차피 못 봄. D-7상 진실은 서버 transcript. 15번 리스크 #1(ko-KR 품질)도 같이 사라짐 | 반영 |
| 2 | Vision OCR(D-6) | 0.5 | 붙여넣기+클립보드 감지가 T-D2 근거대로 가치 70%. 결정 D-6은 유지 | 반영 |
| 3 | 라이브 액티비티 | 0.5 | T-D1 되돌리기 조건이 이미 "액티비티만 남기고"가 아니라 반대지만, 백그라운드 audio 모드만으로 녹음은 계속됨. 심사 시 "녹음 중 표시"는 인앱 ●로 | 반영 |
| 4 | macOS 로컬 알림(D-19) + AWS prod + @SceneStorage | 1.5 | 각각 계약 v1.1(`MACOS`)·12개월 무료 만료·복원 UX와 묶여 v1.1이 자연스러움 | 반영 |
| 5 | Quiz 오답 AI 생성 | 0.5 | 14번 §15 표의 규칙 폴백을 1순위로. 무료 티어 호출 절약 부수효과 | 반영 |
| 6 | macOS TestFlight(11/13 분리) | 1.0 | 코드는 같음. 파이프라인(Mac 인증서·샌드박스·App Store Connect Mac 레코드)만 1주 뒤 | 반영(PM-4) |
| 7 | **다음 컷 1**: 카카오 로그인(Google·Apple만) | 0.5 | T-D6상 Apple 필수, Google은 웹세션 동일 코드. 카카오는 콘솔 등록·심사 변수 | 대기 |
| 8 | **다음 컷 2**: Terraform → 수동 cloud-init 스크립트 | 0.5 | REQ-32 "Terraform"과 충돌하므로 최후 | 대기 |
| 9 | **다음 컷 3**: k6·복구 연습 | 0.5 | 포트폴리오 가치는 있으나 출시 필수 아님 | 대기 |

---

## 4. 리스크 톱 5

| # | 리스크 | 신호(측정) | 완화 | 트리거 시 행동 |
|---|---|---|---|---|
| 1 | **달력 결손 + 버퍼 0**: Day 5 토요일, 추석·대체공휴일·한글날 최대 5일이 S0~S2에 겹침(확인 필요). 공휴일을 쉬면 곧바로 −5일 | 매주 금요일 번다운: 닫힌 이슈 수 < 계획 −1 | 이번 주 안에 사용자가 "공휴일 근무 여부"를 결정. 마일스톤 due는 유지하고 S0을 9/22~9/30로 늘리는 대신 S1을 4일로 압축(#7 Onboarding·Stats를 #6에 합침) | −2일 이상이면 §3-2 순위 7→8→9 순으로 컷 |
| 2 | **iOS 17/macOS 14 경로 미검증**: 실기기 없음, iOS 17 시뮬 런타임을 Xcode 26이 지원하는지 확인 필요. 두 루트 구현 중 하나가 데모 때 처음 실행되는 상황 | CI 매트릭스에 iOS 17 job 없음 / 스냅샷 0장 | 분기를 `AdaptiveRoot` 한 파일로 봉인(다른 화면은 `mwIsWide`만 봄). S6-A에서 iOS 17 스냅샷 서브셋. 런타임 설치 불가면 **최소 타깃을 iOS 18/macOS 15로 상향하는 ADR-0005 개정**을 협의체에 올림 | S1 킥오프까지 런타임 설치 확인 |
| 3 | **AI 4트랙이 시간 블랙홀**: MLX 모델 GB 다운로드, Foundation Models 기기 조건, 무료 티어 RPM/RPD(수치 확인 필요)로 평가 자체가 429 | 하네스 작업 누적 > 1.5일 | 타임박스 1.5일, 평가셋 v0 50문장으로 제한, 미측정은 점수표에 "미측정(사유)"로 기록해도 REQ-10 충족(비교 대상 목록·방법·결과/미결과가 문서로 남음) | 초과 즉시 중단, ADR-0007에 잔여를 v1.1 과제로 |
| 4 | **무료 티어 한도로 S5 실서버 데모·k6 실패** | 라우터 429율 > 5%, circuit breaker 개방 | 사전 우선·10분 재사용(14번 §5-1), k6는 AI 스텁, 데모용 프롬프트 캐시. cost_model에 "무료 한도 초과 MAU" 출력 추가 | 한도 초과면 Groq 폴백, 그래도 부족하면 데모를 캐시된 문장으로 |
| 5 | **문서 오버헤드(REQ-20/21/24/25)가 코드 시간 잠식**: ADR·그림·협의체·일별 로그가 하루 1h를 넘기 시작 | 로그에 "문서" 항목 시간 > 1h/일 | 상한: 협의체 1.5h/스프린트, 그림은 "구조·흐름·결정이 바뀐 이슈"만(REQ-24 "해당 시"), ADR은 한 문단 허용(16번 §1-3) | 2주 연속 초과면 협의체 격주로(REQ-25 상태를 "협의 중"으로 바꾸고 기록) |

차순위(관찰만): 심사 거절(백그라운드 audio 사유·소셜 로그인 동등 배치 — 15번 I-4), 계약 drift(schemathesis), 기밀 텍스트 유출(beforeSend), SFSpeechRecognizer 없이 W-02 체감 저하.

---

## 5. 협의체 운영 규칙 제안 (REQ-25)

**구성**: PM(일정·범위), PO(제품·REQ 대장 소유), AI, iOS, macOS·iPad, Backend, QA + **레드팀 1**(역할 고정 아님, 매번 다른 관점: 비용 / 프라이버시 / 1인 실행 가능성 중 하나를 지정).

**소집 시점** (셋 중 하나)
1. 정기: 각 스프린트 **마지막 날 오후**(데모 직후) = 다음 스프린트 킥오프. 9/26·10/2·10/9·10/16·10/23·10/30 + 11/6 릴리스 레드팀. 사람 시간 상한 1.5h.
2. 설계 변경: REQ 신설·상태 변경, 계약 v1.1 항목 추가, D-n/T-xx 결정 번복(ADR "폐기"), 크리티컬 패스 이슈가 1일 이상 지연.
3. 데모 실패: 스프린트 목표(16번 §3 "목표(데모)")가 녹화되지 않음.

**입력**: BRIEF(공통), 직전 스프린트 로그(`docs/log/`), 번다운(닫힌 이슈/계획), 미결 "확인 필요" 목록.

**산출물** (`council/S{n}.md` 한 파일, 저장소에는 `docs/council/`로 이관 권고)
- 결정 1개 이상, **REQ-20 7항목 형식**(의도→비용→대안→왜→영향→검증→리스크·되돌림). 형식이 빠진 결정은 무효.
- 이슈 변경표(유지/수정/추가/삭제) + 마일스톤 반영은 PM이 `gh` 로 당일 수행.
- 리스크 델타(새로 생긴 것·사라진 것), "확인 필요" 해소 여부.
- ADR 초안 번호 예약(0005~). 그림이 필요한 결정은 archify 1장 링크.

**레드팀 기준** (각 결정마다 체크, 치명 0이어야 착수)
| 등급 | 기준 |
|---|---|
| 치명 | REQ 대장 위반 / 계약 v1.0 변경 요구 / 시크릿·원문 유출 경로 / 유료 결제 발생 / 1인 견적이 스프린트 잔여 시간을 초과 |
| 중요 | 대안이 2개 미만이거나 탈락 근거가 없음 / 검증 방법이 측정 가능하지 않음(숫자·테스트 이름 없음) / 되돌리는 조건에 신호·임계값이 없음 / Apple API 가용성이 문서 MCP로 확인되지 않았는데 "확인 필요" 표기가 없음 |
| 경미 | 영향 파일·문서 절 번호 누락 / 견적이 0.5일 단위가 아님 |
- 레드팀은 결정을 **바꾸지 않고** 등급만 매긴다. 치명·중요는 해당 역할이 같은 세션에서 수정, 재판정 1회.
- 레드팀 산출: `council/S{n}-redteam.md`(결정별 등급표 + 한 줄 사유). 5줄 요약은 `docs/log/`.

**운영 상한**: 세션 사람 시간 1.5h, 결정 수 ≤ 5/세션(넘으면 다음 세션), 에이전트 산출물은 각 2,000자 이내 요약 + 상세 첨부.

---

## 6. 결정 기록 (REQ-20 형식)

### PM-1. 재산정 원칙: 기한·인원 고정, 범위를 자른다
1. **의도**: 11/6 v1.0을 지키면서 신규 REQ 8건을 "설계는 전부, 구현은 핵심만" 반영.
2. **비용**: 돈 0. 시간: 순증 +10.5일 vs 감축 −8.5일 + 토요일 2회 저강도 1.0 + 버퍼 0. 복잡도: v1.1 백로그 14건 증가. 유지보수: 이관 항목마다 `@available`/스텁이 남음.
3. **대안**: (a) 기한 연장 11/20 — 탈락: BRIEF 고정 제약, 취업 일정상 v1.0 늦춰지는 비용이 큼. (b) 품질 감축(테스트·문서 생략) — 탈락: REQ-20/21이 최우선, 포트폴리오 가치가 테스트·기록에 있음.
4. **왜**: 1인 30일에 버퍼가 없다는 건 이미 16번이 안고 있던 문제(달력상 1일 부족)이고, 신규 REQ 중 출시에 필수인 것은 REQ-01·02뿐. 나머지(REQ-04·10~13·22·24·25)는 "설계·측정·문서" 산출물로 충족 가능하므로 구현 범위를 자르는 것이 요구사항을 어기지 않는 유일한 길.
5. **영향**: `docs/16_실행계획.md` §3 표·§4 Day 5/10/11/13/14/18~21/24/26/28~30·S6; 이슈 #2·5·10·11·13·14·18·19·20·21·22·23·24·26·28·29·30 본문; 새 이슈 7건(S0 토요일, S2 AI, S6 5건); `docs/REQUIREMENTS.md` E절에 "재산정 결과" 줄 추가.
6. **검증**: 매주 금 번다운(닫힌 이슈 ≥ 계획−1), 스프린트 데모 녹화 존재, 11/6 `app/v1.0.0` 태그.
7. **리스크·되돌림**: 공휴일을 쉬면 −5일 → §3-2 순위 7~9 컷 발동. S2 말(10/9) 기준 누적 지연 ≥ 2일이면 S3 킥오프에서 S6 ⑤(릴리스 문서)를 11/9로 미루는 안을 재심의.

### PM-2. iOS 17/macOS 14 폴백 범위: 루트 뷰만 이중 구현, 온디바이스 AI·STT는 26+ 전용이고 v1.0 미포함
1. **의도**: REQ-02(도달)를 지키되 분기 지점을 한 곳으로 봉인.
2. **비용**: 시간 +1.0(AdaptiveRoot) +0.5(타깃·ADR) +1.0(S6 스냅샷). 복잡도: 루트 2벌. 유지보수: iOS 18 점유율이 충분해지면 SplitView 경로 삭제 가능.
3. **대안**: (a) SFSpeechRecognizer 폴백까지 구현 — 탈락: +1.0일, 1분 제한·네트워크 의존(Apple 문서), D-7상 미리보기는 부가 가치. (b) 최소 타깃 iOS 18/macOS 15 — 탈락: REQ-02가 17/14를 명시. 단 iOS 17 시뮬 런타임을 Xcode 26이 지원하지 않으면(확인 필요) 이 대안을 협의체에 재상정.
4. **왜**: 12번 설계에서 26 전용 API는 `sidebarAdaptable`(18+), `SpeechAnalyzer`(26+), Foundation Models(26+)뿐이고 `@Entry`는 13+로 확인됨. 첫째만 v1.0 필수 UI, 나머지 둘은 14번 §15·T-D3가 이미 "v1.1 후보/축소 가능"으로 둔 것이라 v1.0에서 빼도 설계 결정을 뒤집지 않는다.
5. **영향**: `Project.swift` 전 타깃 deploymentTargets; 12번 §8 "루트 뷰는 하나" 문구 → "루트 래퍼 하나, 구현 둘"; 12번 §9 Layout `AdaptiveRoot`; 15번 T-C2 폐기·ADR-0005; 이슈 #2·#10·#19.
6. **검증**: 스냅샷 iOS 17 시뮬 서브셋(S6-A) + iOS 26 전체; `NavigationTests`가 두 루트에서 같은 `paths` 동작을 통과; CI에 iOS 17 job 1개.
7. **리스크·되돌림**: 런타임 설치 불가 → S1 킥오프에서 최소 타깃 18/15로 개정. iOS 17 시뮬에서 크래시 회귀 → 해당 화면만 `@available`로 26+ 전용 처리하고 릴리스 노트에 명시.

### PM-3. AI 4트랙(REQ-10~13): v1.0 산출물은 "평가 하네스 + 점수표 + 비용 모델 + ADR", 프로덕션 라우터는 API 무료 티어만
1. **의도**: "전부 비교"와 "무료만"을 동시에 만족하면서 크리티컬 패스를 건드리지 않는다.
2. **비용**: 돈 0(Gemini·Groq 무료, MLX 로컬, Foundation Models 로컬). 시간 1.5일 타임박스(+0.5 실측 갱신). 복잡도: 어댑터 인터페이스 1개. 저장소: 모델 가중치는 커밋 금지(`.gitignore`).
3. **대안**: (a) 4트랙 전부 라우터에 제공자로 꽂기 — 탈락: 서버(리눅스 VM)에서 MLX·Foundation Models는 실행 불가, sLLM은 학습 데이터 미확보. (b) 평가를 S6로 미룸 — 탈락: 14번 §15 "평가셋 없이는 모델을 바꾸지 않는다"라 라우터 결정(ADR-0007)이 S2에 필요.
4. **왜**: 14번 §15 순서(평가셋 → 라우터 → 온디바이스 → 파인튜닝)를 그대로 따르면 v1.0 안에 닿는 건 앞 두 단계뿐이다. 온디바이스·MLX는 "측정"까지, sLLM은 "베이스 모델 하한선 측정"까지가 30일에 맞는 깊이다.
5. **영향**: `eval/run.py`, `eval/cost_model.py`, `eval/prices.yml`, `eval/README.md`; 14번 §15에 4트랙 표(트랙·실행 위치·측정 결과·v1.0 역할); ADR-0007; `docs/diagrams/ai-tracks.svg`; 이슈 #11·새 S2 이슈·#29.
6. **검증**: `make eval` 점수표에 4행(미측정은 사유 명시), `pytest eval/` 통과, `make cost MAU=1000`이 월 원가·손익분기를 출력, 단가표에 날짜·출처.
7. **리스크·되돌림**: 1.5일 초과 → 즉시 중단, 미측정 기록. 무료 한도 수치(확인 필요)가 예상보다 낮으면 cost_model의 "무료 한도 초과 MAU"를 첫 유료 전환 지표로 REQ-12에 반영.

### PM-4. macOS TestFlight를 11/13(v1.0.1)로 분리, 11/6은 iOS·iPad TestFlight + 서버 v1.0
1. **의도**: S6 1.0일을 확보하고 iPad(REQ-01)를 11/6 안에 넣는다.
2. **비용**: 시간 −1.0(S6). 돈 0. 복잡도: 릴리스 태그 2개(`app/v1.0.0` iOS, `app/v1.0.1` +Mac). 포트폴리오: "양 플랫폼 동시" 문구는 11/13부터.
3. **대안**: (a) 양 플랫폼 11/6 유지 — 탈락: 버퍼 −1.0. (b) macOS를 v1.1로 — 탈락: D-0·T-E1이 macOS를 1급 플랫폼으로 두었고 코드는 이미 같음, 파이프라인만 문제.
4. **왜**: iPad는 iOS 빌드에 포함되어 추가 배포 비용이 0이지만, Mac은 인증서·샌드박스·App Store Connect 레코드·XCUITest가 별도다. 코드 완성도는 같으므로 1주 뒤 배포가 제품 리스크를 키우지 않는다.
5. **영향**: 16번 §3 S6 목표 문구, §2-5 릴리스 태그; 12번 §15 D-25 유지; 이슈 S6 ④·⑤.
6. **검증**: 11/6 iOS TestFlight 빌드 번호 존재, 11/13 Mac TestFlight 빌드 존재, 두 빌드의 커밋 해시 동일(코드 차이 0 증명).
7. **리스크·되돌림**: S5까지 여유가 1.0일 이상 생기면 11/6 동시 배포로 복귀(협의체 S6 킥오프에서 판단).

### PM-5. 협의체 운영은 §5 규칙으로 고정하고 사람 시간 1.5h/스프린트를 상한으로 둔다
1. **의도**: REQ-25를 지키면서 리스크 5(문서 오버헤드)를 통제.
2. **비용**: 시간 0.25일 × 6 = 1.5일. 돈 0. 복잡도: `council/` 파일 2종/스프린트.
3. **대안**: (a) 매 이슈마다 협의 — 탈락: 30회 × 1h = 5일. (b) S0·S3·S6 3회만 — 탈락: REQ-25 "매 스프린트" 명시.
4. **왜**: 결정이 실제로 바뀌는 시점은 스프린트 경계와 설계 변경 때뿐이고, 레드팀 기준을 표로 고정하면 세션당 1.5h로 수렴한다.
5. **영향**: 16번 §5 "매일 같은 순서"에 7번 항목(스프린트 말 협의체) 추가; `docs/council/` 신설 권고; REQ-25 "영향" 칸에 규칙 링크.
6. **검증**: 스프린트마다 `council/S{n}.md`·`S{n}-redteam.md` 존재, 치명 0 상태로 킥오프, 로그에 소요 시간 기록 ≤ 1.5h.
7. **리스크·되돌림**: 2회 연속 1.5h 초과 → 격주로 완화(REQ-25 상태 "협의 중"으로 변경·날짜 기록).

---

## 7. 사용자 결정 요청 (확인 필요 3건)
1. **공휴일 근무 여부**(추석 9/24~26·대체 9/28·10/5·한글날 10/9 — 날짜 자체도 확인 필요). 쉬면 §3-2 순위 7~9 컷을 미리 승인할지.
2. **Xcode 26에서 iOS 17 시뮬레이터 런타임 설치 가능 여부** — 불가면 최소 타깃 18/15 개정(PM-2 되돌림).
3. **개발 Mac의 macOS 버전·Apple Intelligence 활성 여부** — Foundation Models 실측(S2-E) 가능 여부 결정.
