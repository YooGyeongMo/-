# 요구사항 대장 (REQ Registry)

> 이 파일은 잊으면 안 되는 것의 단일 목록이다. 모든 이슈·PR·ADR은 여기 ID(`REQ-xx`)를 참조한다.
> 상태: 확정 / 협의 중 / 폐기. 바뀌면 줄을 지우지 않고 상태와 날짜를 바꾼다. "왜"가 없는 요구사항은 등록하지 않는다.

## A. 제품·플랫폼
| ID | 요구사항 | 왜 (사용자 의도) | 상태 | 영향 결정 |
|---|---|---|---|---|
| REQ-01 | 앱은 **iPhone, iPad, macOS** 세 폼팩터. iPad는 macOS와 같은 넓은 레이아웃(사이드바·세 열), iPhone은 세로 접기 | 회의실에서 iPad, 자리에서 Mac, 이동 중 iPhone | 확정 2026-09-19 | D-0 수정, D-2 확장(iPad = 넓은 화면) |
| REQ-02 | 최소 지원 **iOS 17 / iPadOS 17 / macOS 14**. iOS 26 전용 기능은 `@available` 게이트 + 폴백 | 도달 범위. "다 대비해" | 확정 2026-09-19 | 15번 T-C2 **폐기·재결정** (26 only → 17+) |
| REQ-03 | UI는 가장 작은 화면(iPhone SE 4.7")부터 가장 큰 화면(Mac 6K, iPad 13")까지 대응. 고정 px 금지 | 모든 기기 | 확정 | SDK 규칙 6 확장, 스냅샷 매트릭스 확장 |
| REQ-04 | 알림은 이후 **watchOS**에도 붙인다. 지금부터 알림 payload·딥링크·복습 카드 모델을 워치가 쓸 수 있게 설계 | 손목에서 퇴근길 복습 알림 | 확정(설계만 v1, 구현 v2) | Notification 스키마, `Domain` 모델 워치 공유 가능 |
| REQ-05 | 웹 브라우저 클라이언트는 v1 범위 밖(계약은 유지) | 한 스택 | 확정 | D-0 |

## B. AI (핵심 기능 "해석하기")
| ID | 요구사항 | 왜 | 상태 | 영향 |
|---|---|---|---|---|
| REQ-10 | 해석 엔진 후보를 **전부** 비교한다: ① 외부 API(OpenAI, Gemini, Claude) ② 온디바이스 Apple Foundation Models(iOS 26+) ③ 로컬 LLM **Apple MLX**(Mac, 4B~8B 양자화) ④ 자체 소형 모델(Karpathy식 nanoGPT/파인튜닝 sLLM) | 최고 성능과 BM을 둘 다 잡기 위해 선택지를 닫지 않는다 | 확정 | 14번 §15 재작성, ADR |
| REQ-11 | 테스트·개발 단계는 **무료 티어만**(Gemini 무료, OpenAI·Claude는 사용자 제공 키, 크레딧 없으면 무료 모델). 유료 결제 없음 | 비용 0으로 검증 | 확정 | 라우터 설정, 키 관리 |
| REQ-12 | 배포 시 성능(품질·지연)과 **BM**(MAU당 원가)을 같이 고려한 라우팅 | 서비스 유지 가능성 | 확정 | 비용 모델 REQ-13 |
| REQ-13 | **MAU당 월 비용을 자동 산정**하는 계산기(입력: MAU, 1인당 해석·녹음·복습 수, 모델 단가 → 출력: 월 원가, 손익분기 가격). 코드로, 수치는 측정값 갱신 | "예측 돈 한 달에 MAU마다 얼마" | 확정 | `eval/cost_model.py` + 대시보드 |
| REQ-14 | 검수된 사전이 AI보다 우선, 인덱스는 서버 재계산, 원문은 SDK로 안 나감 | 품질·기밀 | 확정(기존) | 14번 §5-1, 12번 §10 |

## C. 엔지니어링 원칙
| ID | 요구사항 | 왜 | 상태 | 영향 |
|---|---|---|---|---|
| REQ-20 | 모든 도구·SDK·라이브러리·AI 선택은 **의도 → 비용 → 대안 → 왜** 순서로 먼저 적고 나서 쓴다. 코드보다 문서가 먼저 | 취업 시 설명 가능해야, 나중에 Git Wiki | 확정 (최우선) | ADR 필수, PR 템플릿 "의도" 항목 |
| REQ-21 | 트레이드오프와 배운 점을 매번 기록(ADR + `docs/lessons/`) → Git Wiki로 이관 | 포트폴리오 | 확정 | 16번 §2 |
| REQ-22 | 애니메이션은 코어(타이밍 곡선·물리·지속시간)까지 손댈 수 있고 튜닝 가능한 **모듈**(`MwonmalUI/Motion`)로. 하드코딩 금지 | 디자인 품질 통제 | 확정 | SDK 컴포넌트 |
| REQ-23 | 구현 전 Swift/Apple 공식 API를 먼저 찾고(문서 MCP), 있는 것을 쓴다. 최적화가 필요해지면 **사용자에게 먼저 말한다** | 바퀴 재발명 금지 | 확정 | 16번 §1-3 |
| REQ-24 | 구조·흐름·결정은 **archify 그림**으로 기록(`docs/diagrams/`) | 시각화로 이해 | 확정 | 이슈마다 그림 1장 이상(해당 시) |
| REQ-25 | 역할별 에이전트 협의체(PM, PO, AI, iOS, macOS·iPad, Backend, QA)가 결정을 만들고 **레드팀**이 검증한 뒤 착수 | 결정을 더 터프하게 | 확정 | 매 스프린트 시작·설계 변경 시 |
| REQ-26 | 반복되는 것은 모듈·SDK로, Tuist로 강제 | 재사용 | 확정(기존) | ADR-0001 |
| REQ-27 | 커밋에 AI 공동 저자 표기 없음, 시크릿·개인정보 커밋 금지(공개 저장소) | | 확정 | gitleaks |

## D. 운영·비용
| ID | 요구사항 | 왜 | 상태 | 영향 |
|---|---|---|---|---|
| REQ-30 | 무중단 CI/CD(블루·그린), GitHub Projects 스프린트·마일스톤·일별 이슈, 릴리스 노트 자동 | | 확정 | ADR-0002·0003 |
| REQ-31 | Sentry(깨짐·지연)와 Amplitude(사용·리텐션)의 목적을 분리하고, 붙이는 이유·비용·PII 정책을 먼저 적는다 | | 확정 | 16번 §1-2 |
| REQ-32 | 클라우드는 무료 티어 우선(OCI Free, AWS 무료 12개월, R2), Terraform | | 확정 | 14번 §14 |

## E. 재결정 대기 (REQ 변경으로 흔들린 기존 결정)
| 기존 결정 | 흔든 REQ | 협의체에서 정할 것 |
|---|---|---|
| 15번 T-C2 "iOS 26 only" | REQ-02 | 17+ 지원 시 SpeechAnalyzer/Foundation Models/`sidebarAdaptable`/`@Entry` 폴백 설계, 스냅샷 매트릭스 |
| 12번 D-0 "웹 → macOS" | REQ-01 | iPad를 macOS 레이아웃군에 넣는 규칙(`horizontalSizeClass` + 플랫폼), iPad 멀티태스킹 |
| 14번 §15 AI 라우터 | REQ-10~13 | MLX·nanoGPT·Foundation Models·API 4트랙 평가 계획, 비용 모델 |
| 12번 D-19 알림 | REQ-04 | 워치 확장 가능한 알림 스키마 |
| 12번 §9 SDK 규칙 6 Dynamic Type | REQ-03 | 최소~최대 화면 레이아웃 규칙, 스냅샷 기기 매트릭스 |
